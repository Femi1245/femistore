EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Deployed Next.js API (live tokens + YouTube search)
EXPO_PUBLIC_API_URL=https://itunes-mu.vercel.app

# Supabase → Auth → URL Configuration — also add your mobile redirect:
#   zumelia://auth/callback
# In Expo Go, run the app once and add the exp://... URL shown in Metro logs if OAuth fails.
